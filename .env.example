# Copy to .env for local overrides. Do not commit real secrets.
STOCKER_CONFIG=configs/research.example.yaml
STOCKER_MODE=paper
STOCKER_LOG_LEVEL=INFO

# Broker placeholders for future work. Keep real values outside git.
STOCKER_BROKER_PROVIDER=placeholder
STOCKER_BROKER_ACCOUNT_ID=
STOCKER_BROKER_API_KEY=
