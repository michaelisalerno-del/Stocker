# Copy to .env for local overrides. Do not commit real secrets.
STOCKER_CONFIG=configs/research.example.yaml
STOCKER_MODE=paper
STOCKER_LOG_LEVEL=INFO

# Data vendor tokens. Keep real values outside git.
EODHD_API_TOKEN=

# IB Gateway/TWS authentication stays in IB Gateway/TWS. Non-secret connection
# settings are in configs/ibkr.example.yaml; do not commit real account details.
