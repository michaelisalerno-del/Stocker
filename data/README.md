# Data Directory

Data and generated research artifacts are intentionally gitignored.

- `raw/`: immutable vendor or broker exports.
- `processed/`: cleaned datasets after validation.
- `features/`: feature matrices generated from processed data.
- `reports/`: charts, metrics, notebooks exports, and backtest reports.

Keep secrets and broker exports with account identifiers out of git.
