# FIRST4 PAPER execution conventions deployed

**Latest recovery, 2026-09-23 21:40 UTC:** restarted the existing PAPER Gateway through its authenticated restart flow. It returned to the login screen before reconnecting; FIRST4 then reconciled to `DUP655399`. Two subsequent non-transmitting checks (21:37 and 21:39 UTC) no longer reported error 10197 and returned the BAG price increment of $0.01 per share. This does not establish that another device was logged in. The original 23:45 UTC automatic restart setting was restored and verified in Gateway. All services are healthy; the 32 focused FIRST4 tests pass. The only remaining broker-check failure is fresh, positive two-sided real-time option quotes, unavailable during these after-hours checks. Entries remain disarmed and LIVE disabled. No orders or fills were observed. [Recovery evidence](first4-recovery-evidence.json) supersedes the earlier competing-session diagnosis below. The September 23 continuity block is retained; a new session starts afresh under the existing calendar logic.

On 2026-09-23, code revision `46474ff3b3e070cea7e14642c56a29b207f0e820` was installed and deployed through the existing `stocker-v1` service. `/etc/stocker/v1/first4.yaml` now contains exactly the values in [the configuration file](../configs/first4.example.yaml). These are newly specified PAPER conventions, not synthetic-research validation. [FIRST4.md](FIRST4.md) explains the original source, exact clock/expiry calculation, listing rules, budget and operational differences.

After restart, the service was active and connected/reconciled to PAPER account `DUP655399`, client 81. Effective configuration had no missing settings, `armed: false`, and `live_enabled: false`. The prior ledger was retained with no replay; orders, fills and positions remained zero. Only the intended FIRST4 service was configured/running as an entry manager. The three-scanner observer hashes were unchanged; its sender whitelist excludes order placement. Configuration and consistent ledger backups are in `/var/lib/stocker/backups/first4-paper-46474ff3b3e0`.

Verification: 420 full-suite tests passed (five existing/dependency warnings), 37 focused FIRST4/security tests passed, mypy passed for 106 source files, Ruff lint/format passed, browser/mobile navigation checks passed, and the locked server-only install smoke passed as the service user. A shortened-session check verified the 2026-11-27 13:00 ET stock close produces 12:58 ET close-out. The frozen 1,293 observations and 80 FIRST4 allocations remain unchanged. Independent reviews found two asynchronous broker races; both were fixed with direct regressions (exit quantity recheck after quote waits, and cancellation blocking across simultaneous orders).

The non-transmitting broker checks confirmed the exact PAPER identity, empty exposure/open orders, qualified USD option conIds, multiplier 100, quantity increment/minimum 1 and actual expiry time. An earlier check returned combo increment .01 USD per share; the final check against the deployed config could not repeat it because IBKR reported error 10197, “No market data during competing live session.” The two ATM Ford options in that access check are diagnostic contracts only: they are not signals, admissions or the trading structure. Both order methods were disabled and zero orders were transmitted.

**Remaining prerequisites:** resolve the competing IBKR market-data session reported by error 10197, then repeat `scripts/first4_paper_check.py --config /etc/stocker/v1/first4.yaml` during regular US option hours. The final check has `fresh_realtime_option_quotes: false` and `combo_price_increment: false`, with `OPTION_QUOTES_INVALID_STALE_OR_UNAVAILABLE` and `COMBO_PRICE_INCREMENT_UNAVAILABLE`. New entries remain disarmed. No LIVE session was disconnected or changed. Do not arm until valid real-time quotes and the remaining checks pass. If they pass, set `armed: true`, restart the same service and verify its effective account/configuration; a mid-session restart must still honor frozen first-appearance continuity and cannot replay earlier opportunities. No test trade was placed, and actual broker acceptance, leg fills and realized P&L have not been observed.

The dashboard keeps selected/skipped/unfilled admissions and their reasons. Broker-simulated fills/P&L, actual commissions and reserved fee allowance are reported separately from the timestamped quoted ask-to-bid comparison. Exit submission is at stock close minus 120 seconds, an explicit research-to-execution difference. Missing/failed closing execution remains an operator exception and never becomes a fabricated closed position.

Captured [effective deployment evidence](first4-paper-execution-evidence.json) and [final check against the actual deployed config](first4-paper-data-check.json).

Follow-up at 2026-09-23 21:22 UTC: started the previously failed Gateway window manager and reran the existing daily readiness service successfully (`ready:4002`). No unit or trading configuration was changed; systemd now reports zero failed units. FIRST4, Gateway and its window manager are active. The Gateway UI shows its API and market-data farms connected, with one active API client. No competing IBKR desktop process or browser session was found on this Mac, and only one Gateway process runs on the server.

The [additional non-transmitting diagnostic](first4-readiness-latest.json) again passed PAPER identity/reconciliation, zero open orders and standard option qualification. Individual options reported data type 1 but bid/ask -1 and sizes zero after the regular session; the BAG request returned error 10197. A connected data farm or a recent last-size tick is not evidence of fresh executable bid/ask quotes. No existing LIVE session was disconnected. The outstanding action is to log out of the competing market-data session on the other device (without disrupting unrelated trading), then rerun the existing check during regular option hours. If no such session is open and 10197 persists, the shared-data session needs investigation with IBKR. Settings remain `armed: false` and LIVE disabled; no arbitrary trade was placed.
