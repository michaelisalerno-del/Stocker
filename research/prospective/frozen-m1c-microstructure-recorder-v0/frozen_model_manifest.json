{
  "claims_contract": "contract.json",
  "archetypes": {
    "A1": {
      "confidence_boundary": 0.10239913693629493,
      "label": "prospective hypothesis — not validated",
      "threshold_rule": "CALL when p_up >= 0.5 + boundary; PUT when p_up <= 0.5 - boundary; otherwise ABSTAIN"
    },
    "C1": {
      "confidence_boundary": 0.08831660487389237,
      "label": "comparison only — not validated",
      "threshold_rule": "CALL when p_up >= 0.5 + boundary; PUT when p_up <= 0.5 - boundary; otherwise ABSTAIN"
    },
    "R1": {
      "confidence_boundary": 0.09516361267323831,
      "label": "comparison only — not validated",
      "threshold_rule": "CALL when p_up >= 0.5 + boundary; PUT when p_up <= 0.5 - boundary; otherwise ABSTAIN"
    }
  },
  "contract_version": "frozen-m1c-microstructure-recorder-v0",
  "direction_feature_cutoff": "T-1",
  "fitted_at_runtime": false,
  "m1c": {
    "fresh_episode_rule": "current_probability >= threshold and immediately_preceding_eligible_probability < threshold or no_prior_eligible_checkpoint, with at least 30 minutes since an earlier episode",
    "model_hash": "6f59177a58973d33a24741e3c265e1831bfb6dc07afac17ae371501019bdc5cc",
    "replacement_features_added": false,
    "threshold": 0.488333710794033,
    "trigger_bar_included": true
  },
  "removed_contaminated_features": [
    "signed_pressure",
    "tension",
    "all peer-normalised Group I inputs",
    "all numerical descendants of contaminated peer-slate lineage"
  ],
  "trigger_bar_excluded_from_direction": true,
  "claims_boundary": {
    "a1_frozen_prospective_hypothesis": true,
    "account_balance_access_required": false,
    "broker_account_mutation_allowed": false,
    "c1_frozen_comparison": true,
    "execution_enabled": false,
    "live_orders_allowed": false,
    "m1c_frozen": true,
    "m1c_threshold": 0.488333710794033,
    "microstructure_direction_model_fitted": false,
    "microstructure_features_descriptive_only": true,
    "option_pnl_is_shadow_quote_pnl": true,
    "option_quotes_recorded": true,
    "paper_orders_allowed": false,
    "place_order_method_available": false,
    "position_access_required": false,
    "prospective_collection": true,
    "r1_frozen_comparison": true,
    "record_only": true,
    "research_only": true,
    "strategy_promotion": false
  }
}
